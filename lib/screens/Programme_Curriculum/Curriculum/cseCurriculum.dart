import 'package:flutter/material.dart';

class cseCurriculumPage extends StatefulWidget {
  const cseCurriculumPage({Key? key}) : super(key: key);

  @override
  State<cseCurriculumPage> createState() => _cseCurriculumPageState();
}

class _cseCurriculumPageState extends State<cseCurriculumPage> {
  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
